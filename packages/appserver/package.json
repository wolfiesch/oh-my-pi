{
  "name": "@oh-my-pi/appserver",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin OMP authority adapter for the T4-owned host service",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src", "README.md"],
  "exports": { ".": { "types": "./src/index.ts", "import": "./src/index.ts" } },
  "scripts": {
    "build": "bunx tsc -p tsconfig.json --noEmit",
    "check": "bun run build",
    "test": "bun test test/*.test.ts"
  },
  "dependencies": {
    "@t4-code/host-service": "0.1.30",
    "@t4-code/host-wire": "0.1.30"
  },
  "engines": { "bun": ">=1.3.14" }
}
