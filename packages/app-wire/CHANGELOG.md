# Changelog

## [Unreleased]

### Added

- Added the public, versioned `omp-app/1` protocol SDK with bounded frame decoders and golden fixtures.

### Fixed

- Validate known file, audit, and catalog result fields and support NodeNext `.js` subpath imports from the packed SDK.
